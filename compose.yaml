services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file: .env
    environment:
      DATABASE_PATH: /data/birdbox.db
    volumes:
      - orders:/data
    restart: unless-stopped
volumes:
  orders:
